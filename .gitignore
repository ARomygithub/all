# Ignore everything by default
*

# But keep files with these extensions
!*.cpp
!*.py
!*.sh

# Keep specific files
!kactl.pdf
!content.zip

# Keep directories (so git can traverse them)
!*/

# Keep .gitignore itself
!.gitignore
